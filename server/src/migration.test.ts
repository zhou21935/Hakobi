import { readFileSync } from 'node:fs'; import { resolve } from 'node:path'; import { describe,expect,it } from 'vitest'
describe('migration',()=>{it('has schema security contract',()=>{const sql=readFileSync(resolve(import.meta.dirname,'../../supabase/migrations/20260730000000_create_orders.sql'),'utf8').toLowerCase(); expect(sql).toContain('references auth.users(id) on delete cascade'); expect(sql).toContain('(user_id, created_at desc)'); expect(sql).toContain('create trigger'); expect(sql).toContain('enable row level security'); expect(sql.match(/create policy/g)).toHaveLength(4); expect(sql.match(/auth\.uid\(\)/g)?.length).toBeGreaterThanOrEqual(4)})})
