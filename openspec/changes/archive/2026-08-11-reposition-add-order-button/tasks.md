## 1. 版面行為測試

- [x] 1.1 先為 `Order list create action follows the filtering controls` 增加失敗測試，驗證分類訂單頁與全部訂單頁的「+ 新增訂單」按鈕在 DOM 中位於狀態分頁之後、訂單內容之前，並具有靠右且不滿版的配置；以執行相關 Vitest 測試且在實作前如預期失敗驗證。

## 2. 訂單列表版面調整

- [x] 2.1 調整分類訂單頁與全部訂單頁，使桌面及手機版的「+ 新增訂單」按鈕皆顯示於狀態分頁後、訂單內容前並靠右維持內容寬度，同時保留開啟既有新增表單的行為；以第 1.1 項測試轉為通過驗證。
- [x] 2.2 更新受位置描述影響的空狀態文字，使提示不再指向「右上角」，並以相關頁面測試與文字斷言驗證。

## 3. 完整驗證

- [x] 3.1 執行完整前端測試與 production build，驗證版面變更未破壞訂單建立、搜尋、排序、篩選及建置流程；以 `npm test -- --run` 與 `npm run build` 均成功完成驗證。
