## 1. 響應式排列測試

- [x] 1.1 先為 `Order list create action follows the filtering controls` 更新分類訂單頁與全部訂單頁的失敗測試，驗證共用容器在桌面採同列左右分散排列、手機採上下排列，且按鈕右緣受相同內容寬度約束；以相關 Vitest 測試在版面實作前如預期失敗驗證。

## 2. 響應式版面實作

- [x] 2.1 調整兩個訂單列表頁，使狀態分頁與新增按鈕在桌面版同列、按鈕靠最右並對齊訂單卡片右緣，而手機版維持分頁下一列靠右且按鈕不滿版；以第 1.1 項測試轉為通過驗證。

## 3. 完整驗證

- [x] 3.1 執行完整前端測試與 production build，驗證響應式排列沒有破壞新增表單、狀態篩選及訂單列表；以 `npm test -- --run` 與 `npm run build` 均成功完成驗證。
